replicaCount: 2

fullnameOverride: "user-info-app"

image: nitrodenov/otus:v5.1.3

deployment:
  app: otus5
  targetPort: container-port

service:
  type: NodePort
  port: 9000
  app: otus5
  targetPort: container-port

ingress:
  host: arch.homework